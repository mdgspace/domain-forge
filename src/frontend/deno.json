{
  "tasks": {
    "dev": "deno run -A --node-modules-dir npm:vite@4.0.4",
    "build": "deno run -A --node-modules-dir npm:vite@4.0.4 build",
    "preview": "deno run -A --node-modules-dir npm:vite@4.0.4 preview",
    "serve": "deno run --allow-net --allow-read https://deno.land/std@0.157.0/http/file_server.ts dist/"
  }
}
